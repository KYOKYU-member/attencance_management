<h2><%= @user.name %>の詳細<h2>


<p>名前：<%= @user.name %></p>
<p>カナ：<%= @user.name_kana %></p>
<p>社員番号：<%= @user.employee_number %></p>


<%# confirmの確認画面が出ないので出し方について調べる必要あり。 %>
<%= link_to "非表示にする", hidden_user_path(@user), method: :get, form: { data: {turbo_confirm: "非表示にしていいですか？"}} %>